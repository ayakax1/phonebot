import os

# === НАСТРОЙКИ БОТА ===

# На Render токен берётся из переменной окружения BOT_TOKEN (задаётся в
# настройках сервиса). Локально можно просто вписать токен вместо второго
# аргумента.
BOT_TOKEN = os.environ.get("BOT_TOKEN", "ВСТАВЬТЕ_СЮДА_ТОКЕН_ОТ_BOTFATHER")

# Telegram user_id тех, кому разрешено добавлять телефоны (/addphone)
ADMIN_IDS = [123456789]  # замените на свой user_id

# Папка, где хранятся фото телефонов (на вашем компьютере/сервере)
PHOTOS_DIR = "photos"

# Файл базы данных SQLite
DB_PATH = "phones.db"

# Перезарядка между выдачами (в секундах)
COOLDOWN_SECONDS = 3 * 60 * 60  # 3 часа

# Шанс статуса "Б/У" (в процентах). Оставшееся — "Новый"
USED_STATUS_CHANCE = 85

# Диапазон изменения цены (доля от базовой стоимости)
PRICE_ADJUST_MIN = 0.15  # минимум 15%
PRICE_ADJUST_MAX = 0.50  # максимум 50%

# Порядок редкостей от самой частой до самой редкой
# Вес — относительная "частота" выпадения. Можно менять как угодно.
RARITY_WEIGHTS = {
    "Мусор": 300,
    "Необычный": 220,
    "Редкий": 150,
    "Эпический": 90,
    "Легендарный": 50,
    "Мифический": 25,
    "Хроматический": 12,
    "Коллекционный": 6,
    "Секретный": 3,
    "Космический": 1,
}

# Порядок для красивого вывода/сортировки (от низшей к высшей)
RARITY_ORDER = list(RARITY_WEIGHTS.keys())
